mysql:
  host: xxxx
  port: xxxx
  user: xxxx
  passwd: xxxx
  db: xxxx
  table_name: xxxx
  create_table_sql: CREATE TABLE {} (`id` int auto_increment primary key, `sight_name` varchar(250) DEFAULT NULL, `sight_level` varchar(250) DEFAULT NULL, `sight_area` varchar(250) DEFAULT NULL, `sight_price` varchar(50) DEFAULT NULL, `sight_soldnum` varchar(50) DEFAULT NULL, `sight_hot` varchar(255) DEFAULT NULL, `sight_add` varchar(255) DEFAULT NULL, `sight_point` varchar(255) DEFAULT NULL, `sight_slogen` varchar(255) DEFAULT NULL, `sight_url` varchar(255) DEFAULT NULL) CHARSET=utf8mb4;
  insert_table_sql: INSERT INTO {} (sight_name, sight_level, sight_area, sight_price, sight_soldnum, sight_hot, sight_add, sight_point, sight_slogen, sight_url) VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s)
